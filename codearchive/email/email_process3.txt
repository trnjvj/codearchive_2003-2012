<HTML>
<HEAD>
<META HTTP-EQUIV="Refresh" Content="1, URL=email_process3.php">
<BODY BGCOLOR="#FFFFFF" text="#000000" link="#111111" vlink="#222222" alink="#333333">
<TITLE>Import URLs</TITLE>
<font face=verdana size=1>
<?
set_time_limit(0);
$mcount24=0;
$nmn=0;
$Host = "localhost";
$User = "root";
$Password = "";
$DBName = "world";
$Tablename = "T1";
$Link = mysql_connect ($Host, $User, $Password) or die ("Cannot connect to the database.\n");
mysql_select_db ("$Tablename");

$Query120 = "SELECT a,b FROM `archipelago_websites_state` WHERE 1;";
$Result120 = mysql_db_query($DBName, $Query120, $Link);
while ($Row120 = mysql_fetch_array ($Result120))
	{
	$mbb = $Row120[0];
	$mcc = $Row120[1];
	$Query121 = "SELECT * FROM `herbage_email_list2` ORDER BY a LIMIT $mbb,500;";
$Result121 = mysql_db_query($DBName, $Query121, $Link);
while ($Row121 = mysql_fetch_array ($Result121))
	{
	$ma = $Row121[0];
	$mb = $Row121[1];
	$mc = $Row121[2];
	
	$ma1 = strlen($ma);
	$mj = 0;
	for ($i = 0; $i <= $ma1; $i++) {
		$mi = substr($ma, $i, 1);
		
		if ($mi=='a') {$mj++;}
		if ($mi=='e') {$mj++;}
		if ($mi=='i') {$mj++;}
		if ($mi=='o') {$mj++;}
		if ($mi=='u') {$mj++;}
		if ($mi=='y') {$mj++;}
		}
	$mScore = intval(($ma1/$mj)*100);
	
	
	$mok=0;
	if ($mScore>500) {$mok=1;}
	if ($mScore<200) {$mok=1;}
	
	
	if ($mok==1) {
	print "<font size=2><b>$ma</b></font> ";
	} else {
	if ($mb=="0") {
	print "$ma ";
	$mcount24++;
	if ($mcount24>23) {$mcount24=0;}
	
	$Query123 = "INSERT INTO `herbage_email_list3` ( `a` , `b` , `c` ) VALUES ('$ma', '$mb', '$mc');";
	$Result123 = mysql_db_query($DBName, $Query123, $Link);
	}
	}
		}
	
	
	$moa = $ma;
	}
	
	


$mbb=$mbb+500;
$Query112 = "UPDATE `archipelago_websites_state` SET a='$mbb';";
$Result112 = mysql_db_query($DBName, $Query112, $Link);

mysql_close ($Link);
?>
