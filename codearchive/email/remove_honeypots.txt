<HTML><HEAD>
<META HTTP-EQUIV="Refresh" Content="1, URL=remove_honeypots.php">
<font face=verdana size=1>
<?
set_time_limit(0);
$nmn=0;
$Host = "localhost";
$User = "root";
$Password = "";
$DBName = "world";
$Tablename = "T1";
$Link = mysql_connect ($Host, $User, $Password) or die ("Cannot connect to the database.\n");
mysql_select_db ("$Tablename");

$Query110 = "SELECT * FROM `xml_state` WHERE 1;";
$Result110 = mysql_db_query($DBName, $Query110, $Link);
while ($Row110 = mysql_fetch_array ($Result110))
	{
	$ma = $Row110[0];
	$mb = $Row110[1];
	$mc = $Row110[2];
	$md = $Row110[3];
	$me = $Row110[4];
	}

$Query120 = "SELECT * FROM `love3` WHERE b='' LIMIT $mb,1000";
$Result120 = mysql_db_query($DBName, $Query120, $Link);
while ($Row120 = mysql_fetch_array ($Result120))
	{
	$ma = $Row120[0];

	$ma1 = strlen($ma);
	$mj = 0;
	for ($i = 0; $i <= $ma1; $i++) {
		$mi = substr($ma, $i, 1);
		
		if ($mi=='a') {$mj++;}
		if ($mi=='e') {$mj++;}
		if ($mi=='i') {$mj++;}
		if ($mi=='o') {$mj++;}
		if ($mi=='u') {$mj++;}
		if ($mi=='y') {$mj++;}
		}
	$mScore = intval(($ma1/$mj)*100);
	
	
	$mok=0;
	if ($mScore>500) {$mok=1;}
	if ($mScore<200) {$mok=1;}
	if ($mok==0) {} else {
		$Query130 = "UPDATE `love3` SET c='$mScore' WHERE a='$ma';";
		$Result130 = mysql_db_query($DBName, $Query130, $Link);
		print "<br>$ma $mScore";
		
		}
	}

$mbb=$mb+1000;
$Query112 = "UPDATE `xml_state` SET b='$mbb';";
$Result112 = mysql_db_query($DBName, $Query112, $Link);

mysql_close ($Link);
?>
